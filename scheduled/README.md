# Scheduled task launchers (source of truth)

Heath's scheduled tasks live locally at `~/Claude/Scheduled/<taskId>/SKILL.md`, which is read-only from a Claude session. Version them here so the repo stays the source of truth.

Process: update the file here, commit, then hand Heath the copy to save over his local one.
